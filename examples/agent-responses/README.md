# Agent Response Samples

These files are sanitized response examples for each sub-agent.

Use them as a team handoff reference:

- keep the top-level `AgentResponse` shape stable
- keep the evidence section compatible with `libra_validation.py`
- use these files to compare outputs during local smoke tests

Files:

- `disclosure.sample.json`
- `news.sample.json`
- `report.sample.json`
- `profit.sample.json`
- `cost.sample.json`
