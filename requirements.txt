# ── AI / API ─────────────────────────────
anthropic>=0.97.0          # Claude API + Tool Use

# ── 뉴스 수집 ────────────────────────────
feedparser>=6.0.11         # RSS 피드 파싱
beautifulsoup4>=4.12.0     # 뉴스 본문 HTML 파싱
requests>=2.31.0           # HTTP 요청

# ── 공시 데이터 ──────────────────────────
pandas>=2.0.0              # 데이터프레임 처리
pyarrow>=14.0.0            # Parquet 파일 읽기

# ── 환경 설정 ────────────────────────────
python-dotenv>=1.0.0       # .env API 키 로드

# ── 스케줄러 ─────────────────────────────
apscheduler>=3.10.0        # 뉴스 에이전트 30분 간격 실행

# ── 표준 라이브러리 (별도 설치 불필요) ───
# argparse, hashlib, json, math, os
# sqlite3, time, uuid, pathlib, datetime