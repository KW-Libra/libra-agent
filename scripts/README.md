# Scripts

Helper scripts for local setup, release packaging, and repeatable development tasks should live here.

Suggested future files:

- `bootstrap.ps1`
- `run-local-agent.ps1`
- `export-contracts.ps1`
